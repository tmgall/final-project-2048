#include <catch2/catch.hpp>

#include <game_state.h>
#include <game_app.h>

using game::GameState;
using game::GameApp;

TEST_CASE("Constructor initializes game tiles correctly") {
  // Make smaller GameState object and ensure the proper rectangles are generated
}
